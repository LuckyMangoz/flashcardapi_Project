# Database values.
DB_USER=LuckyMango
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=flashcard_project

# Optional. If set, this overrides every DB value above.
# DATABASE_URL=postgresql://LuckyMango:password@localhost:5432/flashcard_project

# Application
DEBUG=True
MAX_CARDS=20
DEFAULT_CARDS_COUNT=5

CARD_GENERATOR=rules
LLM_API_KEY=
LLM_MODEL=